package com.atypon.training.yazan.cccbackend.model;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class UserTest {
//    @Test
//    void testRolesParsing() {
//        String userRole = "USER";
//        String adminRole = "ADMIN";
//
//        assertEquals(User.Role.USER, User.Role.valueOf(userRole),
//                "Cannot parse user role from (string) " + userRole);
//        assertEquals(User.Role.ADMIN, User.Role.valueOf(adminRole),
//                "Cannot parse admin role from (string) " + adminRole);
//    }
}
